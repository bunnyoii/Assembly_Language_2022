CODESEG SEGMENT
    ASSUME CS: CODESEG
    PUBLIC DIVIDE_DOUBLEWORD

DIVIDE_DOUBLEWORD PROC NEAR
    PUSH BX
    PUSH AX
    MOV AX, DX
    MOV DX, 0
    DIV CX
    POP BX
    PUSH AX
    MOV AX, BX
    DIV CX
    MOV CX, DX
    POP DX
    POP BX
    RET
DIVIDE_DOUBLEWORD ENDP

CODESEG ENDS

END
